export function SectionTag({ children }: { children: React.ReactNode }) {
  return (
    <div className="text-[10px] font-bold tracking-[0.32em] uppercase text-gold mb-4">
      {children}
    </div>
  );
}
